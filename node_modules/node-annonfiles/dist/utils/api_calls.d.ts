export interface ResponseType {
    status: boolean;
    data?: {
        file: {
            url: {
                full: string;
                short: string;
            };
            metadata: {
                id: string;
                name: string;
                size: {
                    bytes: number;
                    readable: string;
                };
            };
        };
    };
}
export interface UploadOptions {
    file: string;
    key: string | null;
}
export declare type ProtocolsProxies = "http" | "https" | "socks4" | "socks5";
export interface DownloadOptions {
    proxy?: string | null;
    protocol?: ProtocolsProxies | null;
}
export declare class AnnonFilesAPIHandler {
    endpoint: string;
    client: string;
    constructor();
    upload(options: UploadOptions): PromiseLike<ResponseType>;
    getInfo(id: string): PromiseLike<ResponseType>;
    download(id: string, options?: DownloadOptions): PromiseLike<string>;
}
